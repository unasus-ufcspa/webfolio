<!DOCTYPE HTML>
<html lang="en-US">
<head>
<!DOCTYPE html> 

        <title>{% block title %}WEBFOLIO!{% endblock %}</title>
        {% block stylesheets %} 
                <link rel="stylesheet" type="text/css" href="{{ asset('bundles/css/bootstrap.min.css') }}" />
        {% endblock %} 
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head> 

        {% block body %} 
            {{ parent() }}
        <div class="container"> 
            <header class="row"> 
                <div class="col-md-6">
                       <h1>WEBFOLIO - Login</h1> 
            {{ form_start(form) }}

            {{ form_widget(form, {'class': 'col-md-6 control-label'}) }}

            {{ form_end(form) }}
</div>
            </header> 
        </div>

{% endblock %}


</html>
    
